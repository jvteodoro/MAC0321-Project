task('start', group: 'npm', type: Exec) {
    executable 'npm'
    args '--prefix', './app', 'run', 'start'
}

task('build', group: 'npm', type: Exec) {
    executable 'npm'
    args '--prefix', './app', 'run', 'build'
}
